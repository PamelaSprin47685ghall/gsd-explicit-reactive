{
  "id": "explicit-reactive",
  "name": "Explicit Reactive (DAG)",
  "version": "5.0.0",
  "description": "Overrides parallel dispatch with deterministic explicit DEPS.json DAG declarations. Validates, detects cycles, drives background task agents.",
  "tier": "community",
  "requires": {
    "platform": ">=2.29.0"
  },
  "provides": {
    "hooks": [
      "session_start"
    ]
  }
}
