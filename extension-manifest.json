{
  "id": "explicit-reactive",
  "name": "Explicit Reactive (Explicit Waves)",
  "version": "3.0.0",
  "description": "Overrides parallel dispatch to use deterministic explicit WAVES.json instead of implicit file dependencies. Modularized following gsd-context-prune architecture patterns.",
  "tier": "community",
  "requires": {
    "platform": ">=2.29.0"
  },
  "provides": {
    "hooks": ["session_start", "command"]
  }
}
