{
  "id": "explicit-reactive",
  "name": "Explicit Reactive",
  "version": "2.0.0",
  "description": "Overrides parallel dispatch to use deterministic explicit WAVES.json instead of implicit file dependencies.",
  "tier": "community",
  "requires": {
    "platform": ">=2.29.0"
  }
}
