{
  "name": "gsd-explicit-reactive",
  "version": "5.0.0",
  "description": "Explicit DEPS.json DAG-based parallel task dispatch engine — declaration-driven, zero monkey-patch, GSD-native",
  "type": "module",
  "exports": "./index.js",
  "scripts": {
    "test": "node --test test/*.test.js",
    "test:watch": "node --test --watch test/*.test.js"
  },
  "gsd": {
    "extension": true
  },
  "pi": {
    "extensions": [
      "index.js"
    ]
  }
}
